% Insight figures now live in sections/04_Experimental_Results.tex
% (signed histogram, |w| zoom, and per-layer sparsity bars).
% This file is kept as a pointer so the generator README stays valid.
% Do not \input{} it: that would duplicate fig:weight_hist,
% fig:weight_hist_zoom, and fig:sparsity_layers.
