\begin{abstract}
As wireless communication systems advance toward \gls{6G} \gls{RAN}, \gls{DL}-based neural receivers are emerging as transformative solutions for \gls{PHY} processing, delivering superior \gls{BLER} performance compared to traditional model-based approaches. Practical deployment on resource-constrained hardware, however, requires joint reduction of weight and activation precision to lower compute and memory cost without sacrificing reliability. In this paper, we develop a \gls{QAT} methodology for a standard-compliant MU-\gls{MIMO} neural receiver that combines low-bit weight quantization (INT8/INT4 and FP8/FP4 microformats) with INT8 asymmetric post-\gls{ReLU} activation quantization, and optionally 50\% magnitude pruning. On a dual-user DoubleTDL-low uplink, 8-bit weight+activation \gls{QAT} matches weight-only and \texttt{FP32} receivers within $0.05$\,dB at 10\% and 1\% \gls{BLER}, while an analytic cost model shows that the same stack cuts bit-operations by up to $16\times$ and shrinks the weight footprint by $3.8\times$ (8-bit) to $7.1\times$ (4-bit). At 4~bits, FP4~(E2M1) remains far more robust than INT4 under both activation quantization and pruning. These results position joint weight--activation \gls{QAT} as a key enabler of low-complexity neural \gls{PHY} inference for \gls{6G}.
\end{abstract}

%
\begin{keywords}
Deep Learning, Neural Receivers, quantization aware training, activation quantization, resource efficient machine learning
\end{keywords}

%
\begin{comment}
    for IEEE submission portal :
As wireless communication systems advance toward Sixth Generation (6G) Radio Access Networks (RAN), Deep Learning (DL)-based neural receivers are emerging as transformative solutions for Physical Layer (PHY) processing, delivering superior Block Error Rate (BLER) performance compared to traditional model-based approaches. Practical deployment on resource-constrained hardware, however, requires joint reduction of weight and activation precision to lower compute and memory cost without sacrificing reliability. We develop Quantization-Aware Training (QAT) for a standard-compliant MU-MIMO neural receiver that combines low-bit weight quantization (INT8/INT4 and FP8/FP4) with INT8 asymmetric post-ReLU activation quantization, and optionally 50% magnitude pruning. On DoubleTDL-low, 8-bit weight+activation QAT matches weight-only and FP32 within 0.05 dB at 10% and 1% BLER, while an analytic cost model shows up to 16× bit-operation reduction and 3.8×--7.1× real weight compression. At 4 bits, FP4 (E2M1) remains far more robust than INT4. These results position joint weight--activation QAT as a key enabler of low-complexity neural PHY inference for 6G.
\end{comment}
%
